[pytest]
testpaths = tests
markers =
    unit: isolated business logic without database or live service access
    integration: backend components using PostgreSQL, the filesystem, HTTP, or WebSockets
    system: cross-feature user journeys through the application API
    security: authentication, authorization, validation, and rate-limit safeguards
    stress: small deterministic concurrent-load checks for the course project
